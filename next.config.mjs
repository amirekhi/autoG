/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ["cdn.imagin.studio" , 'firebasestorage.googleapis.com' ,'randomuser.me']
    }
    
};

export default nextConfig;
